# Storyboard

## Scene 1 - Parse Every Chunk (0.0-4.2s)

Hero frame: a large code surface on the left contains the full schema-shaped object with empty
defaults while text fills progressively. A right rail shows source chunks, bytes consumed, and the
current snapshot revision. A blue scan line and live caret connect the two zones.

Transition: incoming Scene 2 reveals from the right with a hard vertical rule and 0.45s overlap.

## Scene 2 - React At Completion (3.75-8.0s)

Hero frame: completed JSON paths appear in a left event ledger; the current object remains visible
on the right. `$.metrics.risk` resolves to `"medium"`, then a green conditional decision appears:
`Continue release checks`. Child paths land before their parent.

Transition: Scene 3 expands upward from the policy rail with a 0.45s structural wipe.

## Scene 3 - Choose The Cadence (7.55-11.8s)

Hero frame: four policy lanes share the same payload timeline. Chunk pulses frequently, value emits
at completed boundaries, bytes emits at thresholds, and final holds until the end. Counts and a
single blue playhead make the behavioral difference immediately legible.

Transition: Scene 4 opens through the center dashboard frame with a 0.45s inset reveal.

## Scene 4 - Materialize The Interface (10.9-16.0s)

Hero frame: the release-readiness dashboard is complete. Metrics read 82 readiness, 94 coverage,
and medium risk; milestones and owners have appeared; the completion ledger remains visible; the
final recommendation reads `SHIP` with its operational dependency. Hold the resolved state before a
short final fade.
